name: Build Kernel

permissions:
  contents: write
  actions: write

on:
  workflow_call:

jobs:
  # Xiaomi
  build-renote8-a13:
    name: "Build RENOTE 8 (A13)"
    strategy:
      fail-fast: true
    uses: ./.github/workflows/build-renote8-a13.yml
    secrets: inherit
  
  build-renote10pro-a13:
    name: "Build RENOTE 10 PRO (A13)"
    strategy:
      fail-fast: true
    uses: ./.github/workflows/build-renote10pro-a13.yml
    secrets: inherit
  
  build-pocom3-a13:
    name: "Build POCO M3 (A13)"
    strategy:
      fail-fast: true
    uses: ./.github/workflows/build-pocom3-a13.yml
    secrets: inherit
    
  
